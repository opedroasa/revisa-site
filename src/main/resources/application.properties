spring.application.name=site
# Configuração do banco
spring.datasource.url=jdbc:postgresql://localhost:5432/revisa_db
spring.datasource.username=revisa_user
spring.datasource.password=Ub3rl@nd1@
spring.datasource.driver-class-name=org.postgresql.Driver

# Configuração do JPA
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect

# Flyway (pode deixar ativo, mesmo sem migrations no começo)
spring.flyway.enabled=true
spring.flyway.locations=classpath:db/migration
